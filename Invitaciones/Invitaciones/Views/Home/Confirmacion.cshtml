@model Invitaciones.Shared.Data.EventoInvitado
@{
}
<form asp-action="Confirmacion2" method="get">
	<div class="card col-md-12">
		<div class="card-body">


			<div class="row">
				<div asp-validation-summary="ModelOnly" class="text-danger"></div>
				<input type="hidden" name="token" value="@ViewBag.token" />
				<div class="col-md-12 text-center">
					<img src="/archivos/logo_invitaciones.png" class="image-responsive" />
				</div>
				<div class=" col-md-12 text-center mt-3">
					<h3>@Html.Raw(Model.IdEventoInvitacionNavigation.Remitente)</h3>
				</div>
				<div class=" col-md-12 text-center">
					<p> @Html.Raw(Model.IdEventoInvitacionNavigation.Saludo)</p>
				</div>
				@* <div class=" col-md-12 text-center">
				<h3>
				<p> @Html.Raw(invitado.IdPersonaNavigation.Trato + " " + invitado.IdPersonaNavigation.Nombre + " " + invitado.TextoAdicional )</p>

				</h3>
				</div>*@
				<div class=" col-md-12 text-center">

					@Html.Raw(Model.IdEventoInvitacionNavigation.Motivo)

				</div>
				<div class=" col-md-12 text-center mt-3">

					@Html.Raw(Model.IdEventoInvitacionNavigation.Fecha)

				</div>
				<div class=" col-md-12 text-center mt-3">
					<p>
						@Html.Raw(Model.IdEventoInvitacionNavigation.Lugar)
					</p>
					<p>
						@Html.Raw(Model.IdEventoInvitacionNavigation.Direccion)
					</p>
					<p>
						@Html.Raw(Model.IdEventoInvitacionNavigation.Tenida)
					</p>



				</div>

			</div>
		</div>
		<div class="card-footer col-md-12">
			<input type="submit" value="Confirmar" class="btn btn-primary" />

		</div>

	</div>


</form>