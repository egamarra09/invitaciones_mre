@model Invitaciones.Shared.Data.UsuariosTipo

@{
    ViewData["Title"] = "Details";
}

<div class="card ">
<div class="card-header">
    <h3 class="card-title">Usuario Tipo</h3>
</div>
    <dl class="card-body">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.UsuarioTipo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.UsuarioTipo)
        </dd>
        <div>
    <a asp-action="Edit" class="btn btn-primary" asp-route-id="@Model?.IdUsuarioTipo">Editar</a> |
    <a asp-action="Index" class="btn btn-secondary">Volver</a>
</div>
    </dl>
</div>

