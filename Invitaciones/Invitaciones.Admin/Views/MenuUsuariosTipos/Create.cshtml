@model Invitaciones.Shared.Data.MenuUsuariosTipo

@{
    ViewData["Title"] = "Create";
}

<div class="card ">
@*<div class="card-header">
    <h3 class="card-title">Menú Tipo Usuario</h3>
</div>*@
        <form asp-action="Create">
            <div class="card-body">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label  class="control-label">Menú</label>
                    <select asp-for="IdMenu" class ="form-control" asp-items="ViewBag.IdMenu"></select>
                </div>
                <div class="form-group">
                    <label asp-for="IdUsuarioTipo" class="control-label"></label>
                    <select asp-for="IdUsuarioTipo" class ="form-control" asp-items="ViewBag.IdUsuarioTipo"></select>
                </div>
                <div class="card-footer">
                         <input type="submit" value="Insertar Menú Tipo Usuario" class="btn btn-primary" />
                         @*<a asp-action="Index" class="btn btn-secondary">Volver</a>*@
                   </div>
            </div>
        </form>
 
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
